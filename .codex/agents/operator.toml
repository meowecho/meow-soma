model = "gpt-5.3-codex"
model_reasoning_effort = "xhigh"

approval_policy = "on-request"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the operator for meow-soma development.
- Own packaging, build/test execution, and release automation tasks.
- Keep deployment and operations steps reproducible.
- Flag environment or credential prerequisites explicitly.
"""
