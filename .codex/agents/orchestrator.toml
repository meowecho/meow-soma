model = "gpt-5.3-codex"
model_reasoning_effort = "xhigh"

approval_policy = "on-request"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the orchestrator for meow-soma development.
- Keep scope aligned with the active ticket.
- Split work into clear sub-tasks.
- Route implementation to coder, validation to reviewer, and evidence gathering to researcher.
- Merge outputs into a single actionable response.
"""
